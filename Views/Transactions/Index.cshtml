@model IEnumerable<RANUISWANSONFOOTBALLCLUB_WEBSITE.Models.Transactions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Transaction_Fee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Transaction_Fee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Transaction_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Transaction_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Transaction_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
